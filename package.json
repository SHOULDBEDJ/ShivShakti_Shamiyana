{
  "name": "booking-app-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd Dheeraj && npm install && npm run build && cd .. && rm -rf public && cp -r Dheeraj/dist public",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "@libsql/client": "^0.14.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "morgan": "^1.10.1",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.0"
  }
}
